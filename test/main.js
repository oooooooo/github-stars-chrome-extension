'use strict';

var assert = require('power-assert');

describe('test', function() {
  it('test 1', function() {
    assert.equal(1 + 1, 2);
  });
  it('test 2', function() {
    assert.equal('hello', 'hello');
  });
});
